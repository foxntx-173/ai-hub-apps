# ==========================================
# VERSION DU CODE : v2.6-CACHED-GRADLE92-SCAN
# ==========================================
name: Build All Android APKs

on:
  workflow_dispatch:

jobs:
  build-all:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Cache Gradle 9.2 Binaries & Dependencies
        uses: actions/cache@v4
        id: gradle-cache
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-9.2-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-9.2-

      - name: Install Gradle 9.2 Binaries & Build with Scan/Debug
        run: |
          cd geniex_chat_android
          
          # Contournement du fichier partagé
          mkdir -p _shared/android
          touch _shared/android/common.gradle
          
          # Téléchargement manuel conditionnel si Gradle 9.2 n'est pas déjà présent via le cache
          if [ ! -d "gradle-9.2" ]; then
            echo "Gradle 9.2 non trouvé localement ou dans le cache distant. Téléchargement en cours..."
            wget https://services.gradle.org/distributions/gradle-9.2-bin.zip
            unzip -q gradle-9.2-bin.zip
          else
            echo "Gradle 9.2 détecté via le cache."
          fi
          export PATH="$PWD/gradle-9.2/bin:$PATH"
          
          # Vérification de la version active
          gradle -v
          
          # Compilation avec le wrapper et les options d'information, debug et scan
          gradle wrapper --gradle-version 9.2
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon --info --debug --scan

      - name: Upload APK as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: geniex-chat-apk
          path: '**/build/outputs/apk/debug/*.apk'
